# くそざこ体験版 仕様 v1.1（再復旧・確定差分反映）

> 最終更新: 2025-11-06（JST） / 体験版に必要な**全仕様**を統合。くそざこ感重視＆実装軽量。
> 表記方針: **ちびわふ周り＝ひらがな多め**／**敵名・UI等は通常RPG表記**。

---

## 1) コンセプト & ゲームループ & 勝敗条件（体験版）
- **コンセプト**: くそざこたちが何回負けてもはいあがる。無双は不可、**立ち回りで打開**。
- **ループ**: 《戦闘》↔《キャンプ》（拠点強化＆ショップ）。体験版のゴールは**ステージ1クリア**。
- **勝利**: **ドラゴン撃破 *かつ* 敵拠点全封鎖**（順不同。片方だけ達成してもランは継続し、両条件を満たすまで敵は湧き続ける）。
- **敗北**: **味方拠点が全滅した状態でユウナが戦闘不能**（= 拠点が生きていればユウナが倒れても即敗北しない）。

## 2) プレイヤー & ネームド
- **ユウナ（操作）**: 近接主軸。ダッシュ/ガード/号令（集合・防衛）/回復パルス/簡易魔法/単体回復（最低限）。
- **ミリー（魔法）/マリー（ヒーラー）/ココ（悪魔）**: **親衛隊なし**、戦闘中リスポーンなし。
- **フレンドリーファイア無し**（味方にダメージ/KBは発生しない）。

## 3) ちびわふ（生成・保存・個性）
- **保存最大100体**。生成ごとに **なまえ×称号×職業×性格** で個性。
- **職業**: せんし / まほう / ひーらー（最低3種、尻尾/装飾で識別）。
- **戦闘中リスポーンなし**（体験版）。

## 4) 敵キャラ（通常表記）
- **スライム**（最弱）: 最寄りへ接近・殴る。
- **ゴブリン**: Panic中のちびわふを優先追跡（`panic_target_bonus=+0.30`）。
- **マジシャン**: 中距離魔法。ややカイト。
- **バット**: 高速接近、紙装甲。
- **トリトリ**: **拠点狙い**重視（防衛圧）。
- **ゴーレム（中ボス）**: 高耐久・高防御・遅攻。**ヒット&アウェイで攻略可**。
- **ドラゴン（大ボス）**: 溜め→前方扇形炎（即死級）。予兆あり。

## 5) ステージ1（始まりの草原）
- **地形**: 平地ベース。敵拠点**3つ**（座標は`map_meta.json`）。
- **進行**: 時間ウェーブ→拠点破壊で短縮。一定時間/拠点条件で**ゴーレム**→最終で**ドラゴン**（ドラゴンを先に倒しても残存拠点を封鎖するまでクリアにならない）。
- **クリア**: **ドラゴン撃破 かつ 敵拠点全封鎖**。

## 6) マナ経済（体験版）
- **入手**: 敵撃破（例: スライム1 / ゴブリン1 / マジシャン2 / ゴーレム20 / ドラゴン60）。
- **消費**: 拠点強化、ショップ購入（**リロールなし**・帰還時のみ入替）。
- **ショップ（ルー）**: `mana_gain_up_s` / `mana_cap_up_s` / `chibi_good_title_up` 等の小粒バフに加え、1ランごとに消費される格安の`mana_gain_token`（獲得マナ+10%）を販売。

## 7) 拠点（ダンボール） & 強化
- **強化対象**: HP / 防御 / **ちび生成速度**。追加拠点スロットは高コスト（UIはシンプルツリー、`camp_upgrades.json`）。
- **拠点オーラ**（詳細 → 22章）: 半径内の味方に**与ダメ×1.05／被ダメ×0.90／毎秒+1HP**。

## 8) 性格（二軸・ちびわふはひらがな）
- **ゆうかん**（-2..+2）: びびり / ひかえめ / ふつう / ゆうかん / ちょとつもうしん
- **ちせい**（-2..+2）: おばか / ぽやん / ふつう / なやみがち / かんがえるこ

**挙動反映**
- 逃げHPしきい値（Panic移行）: 0.60 → 0.15（びびり→ちょとつ）
- ターゲット切替ロック（ちせい依存）: 0.8 → 1.8 秒
- AoE回避半径倍率（ちせい依存）: 0.85 → 1.30
- ほんのり補正: 移動-1%〜+1% / 被DoT耐性-1%〜+1% / 近接初動前進+3%（ゆうかん寄り）

## 9) ちびわふAI（0.5秒tick・索敵は“最寄りだけ”）
**行動7種**（どれも“近くに敵きたら攻撃”。**Panic中は攻撃禁止**）
1. てきにとつげき / 2. きょてんこうげき / 3. てきからにげる / 4. ユウナをおいかける / 5. 近くの見方をおいかける / 6. きょてんまもる / 7. うろうろ
- 意思決定: 毎0.5s スコア最大を採用（直前行動に+20%/0.4sヒステリシス）。

## 10) Panic（泣き逃げ）とシナジー
- 入口: `hp_ratio ≤ flee_threshold[ゆうかん]`（0.60/0.45/0.35/0.25/0.15）
- 継続: **最低1.2s**。解除後に再評価。演出: 泣きSE＋泣きエモート。
- **ゴブリン連携**: ちびPanic中 `panic_tag=true` → ゴブリンは+0.30で優先追跡。

## 11) 群れ味付け（軽量Boids）
- コヒージョン: 半径96pxの味方重心へ0.2の補助ベクトル。
- セパレーション: 18px以内で軽く押し返し。
- ちせい↑: AoE回避倍率1.15〜1.30で**やや散る**。

## 12) バトルパラメータ（UIに出る芯）
- **HP / ATK / CD（攻撃間隔） / RNG（射程） / SPD（移動） / CRIT（率/倍率） / ARMOR / RES**
- 体験版は**命中・回避・DEX系なし**（シンプル優先）。

## 13) UI/入力
- 基本移動: WASD。ダッシュ: Space。
- スキル: J=スキル1 / K=ガード / L=号令（防衛） / I=回復パルス / Q=リング。
- 性格アイコンON/OFF: F9。デバッグAIスコア: F10。
- ゲームスピード: F8で**1×→2×→3×→1×**に循環（AI tick・CD・アニメ・SEも同倍率で走らせる。3×時はHPバー更新を0.15s間隔に間引き）。

## 14) マップ/タイル
- **タイル基準**: 32×32（MVP基準）。
- **効果床**: 毒（DoT3/tick）/ スロー（-25%）/ 移動コスト床（×1.6）。
- **描画**: キャラのピクセル密度はタイルに縛られない（48px相当でも可）。

## 15) データファイル構成
- `entities.json` / `spawn_level1.json` / `map_meta.json` / `tile_effects.json` / `skills.json` / `economy.json` / `camp_upgrades.json` / `run_offers.json` / `chibi_personality.json` / `ai_params.json` / `input_bindings.json` / `ui_*`

## 16) テレメトリ
- 毎秒: **ちび死亡数・Panic比率・行動分布** を集計（CSV）。
- ドラゴン戦: 炎ヒット数、拠点HP、クリア/敗北時刻。

## 17) QA完了条件（体験版）
- 平均クリア: 12〜18分。
- Panic: びびり系はHP60%以下で必ず泣き逃げ・攻撃しない。
- ゴブリン: Panicちびを優先追跡。
- 集団: 20体以上で半径≦96pxの小集団形成。
- パフォ: 200体スパイクでも60FPS相当（AI tick 0.5s）。

## 18) 実装順（安全ルート）
1. Panic導入（しきい値・演出・攻撃停止）
2. 7行動フレーム（最寄りのみ／索敵なし）
3. ガード/エスコート（半径ロジック）
4. コヒージョン/セパレーション
5. ゴブリンの“泣き追い”
6. F9トグルの性格表示

## 19) レベル＆成長（救済／線形・低減なし）
- **対象**: 味方のみ（ユウナ／ミリー／マリー／ココ／ちびわふ）。**上限Lv=9999**。敵はレベル無し。
- **思想**: レベルは**救済手段のみ**。その他の失敗救済・ハンデは**無し**。基本は**戦略で勝つ**。

**成長式（Lv1起点）**
- HP: `hp_max = base_hp + hp_per_lv * (lv-1)`
- 与ダメ: `damage *= (1 + dps_per_lv * (lv-1))`
- 攻撃間隔: `atk_cd_s = max(min_cd_s, base_cd_s * (1 - cd_frac_per_lv * (lv-1)))`
- 移動: `speed_u_s = base_speed * (1 + spd_per_lv * (lv-1))`
- クリ率: `crit_chance = base_crit + crit_per_lv * (lv-1)`
> 例係数: `hp_per_lv=0.5 / dps_per_lv=0.0007 / cd_frac_per_lv=0.00018 / min_cd=base*0.6 / spd_per_lv=0.0001 / crit_per_lv=0.000009`

**ちびわふの生成Lv**
- **幾何分布**（低Lv多・稀に高Lv）。初期平均`μ=1.0`、キャンプ訓練で平均のみ底上げ（+0.5 / +1.0 / +2.0）。

**コスト（逓増・割引無し）**
- 味方Lvアップ: `round(10 * 1.6^(lv-1))`
- 訓練所（平均Lv）: 30 / 60 / 120 マナ（仮）

**バランス錨**
- ちび×100 ≒ スライム×1 を**やっと撃破**。
- ユウナLv1はスライムに**基本負け**。**Lv1000**で**ステージ1ソロがギリ可能**（操作・戦略前提）。

## 20) スポーン仕様（体験版・敵は“かなり遅め”）
- **原則**: **ボス以外は拠点封鎖まで無限湧き**／**敵の湧き < ちびの湧き**。

**レート（初期案）**
- ちびわふ: **1.00 体/秒/拠点**（強化で +0.15 段階）
- 敵拠点: **0.10 → 最大0.18 体/秒/拠点**（ウェーブ係数）
- **同時上限**: **全体40体**（拠点別上限なし）
- **構成重み**: **スライム4 / ゴブリン2 / バット2 / マジシャン1 / トリトリ1**
- **拠点封鎖**: その拠点のスポーン**即停止**。場所には**視覚演出だけ**残し、**当たり判定・機能なし**。

**データ例（`spawn_level1.json`）**
```jsonc
{
  "ally_factory": { "rate_per_s": 1.00, "cap": 200 },
  "enemy_bases": [
    { "pos": [12,18], "rate_per_s": 0.10, "rate_max": 0.18, "weights": { "slime":4, "goblin":2, "bat":2, "magician":1, "toritori":1 } },
    { "pos": [32,8],  "rate_per_s": 0.10, "rate_max": 0.18, "weights": { "slime":4, "goblin":2, "bat":2, "magician":1, "toritori":1 } },
    { "pos": [52,24], "rate_per_s": 0.10, "rate_max": 0.18, "weights": { "slime":4, "goblin":2, "bat":2, "magician":1, "toritori":1 } }
  ],
  "global_caps": { "enemies": 40, "chibi": 200 },
  "on_base_sealed": { "stop_spawn": true, "visual_only": true, "no_collision": true }
}
```

## 21) ネームド挙動（体験版・最小）
**共通方針**: **前に出ない**（ユウナ背面150°・距離70–120px）、**危険回避優先**（近接≤72px or 予兆内→退避>攻撃）、**戦闘中リスポーンなし**、**FFなし**、**射線マナーなし**。

**スキルは1つだけ**
- **ミリー**: `fire_ball` — 単体・非貫通・弾速180px/s、`damage:6`、`cd:0.9s`、`range:110px`（地形LOS時のみ）。
- **マリー**: `quick_heal` — 単体回復、`amount:10`、`cd:1.2s`、`range:96px`。優先: **ユウナ＞Panicちび＞他**、HP≦70%で発火。
- **ココ**: `imp_bomb` — 小AoE・弱ダメ＋KB、`radius:40px`、`damage:6`、`kb:360`、`cd:12s`（通路/拠点前の密集のみ）。

**作戦指示（戦闘前プリセット=簡易ガンビット）**
- **後衛固定／拠点守れ／一点集中**（体験版は**選択のみ**。本番はガンビットに拡張）。

**ユウナが倒れた時**
- **拠点が残っていなければ即敗北**。
- 残っていれば **全員「拠点退避」**：最寄り拠点へ移動→中心70–110px背面帯で待機→**拠点オーラ半径内**に敵が入った時のみスキル発火。

**イベント叩き台**
```jsonc
{
  "events": {
    "on_commander_down": [
      { "if": "ally_base.count==0", "do": "defeat()" },
      { "if": "ally_base.count>0",  "do": "set_state(defend_base_fallback)" }
    ]
  },
  "states": {
    "defend_base_fallback": [
      { "if": "true", "do": "move_to_nearest_base()" },
      { "if": "enemy_in_base_aura>=1", "do": "use_skill" },
      { "if": "true", "do": "hold_ring(base_center,70,110,rear=true)" }
    ]
  }
}
```

## 22) 拠点オーラ（体験版）
- **半径**: 128px
- **味方バフ**: 与ダメ×**1.05** ／ 被ダメ×**0.90**
- **微回復**: 毎秒 **+1HP**
- **対象**: 味方全員（ユウナ／ネームド／ちび）
- **重複**: **非スタック**（複数が重なる場合は最大値のみ適用）

**データ例**
```jsonc
{ "radius_px":128, "ally_buffs":{ "atk_mul":1.05, "taken_mul":0.90, "regen_hp_per_s":1.0 }, "stacking":"max_only" }
```

---
（ここでおわり）


## 23) キャンプUI（体験版ミニマム）
- **目的**: 迷わず2分で買って戻る。**2分以内に最低1件購入／クリック3回以内**。
- **タブ構成**: 1) **拠点強化** 2) **訓練所** 3) **作戦**（プリセット選択のみ） 4) **ルーの店**（恒久メタ、リロールなし）。
- **操作要件**: 入室時に**購入可能な最安項目に初期フォーカス**／確認ダイアログ無し（**3秒アンドゥ**）／不足マナ時はボタン非活性＋理由ツールチップ。
- **ショートカット**: `1–4`=タブ切替、`Enter`=購入、`Esc`=戻る。

### 23.1 拠点強化（恒久・逓増）
```jsonc
{
  "base_hp":   { "lv":0, "max":10, "cost0":20, "k":1.6, "delta":80 },
  "base_def":  { "lv":0, "max":8,  "cost0":25, "k":1.6, "delta":0.05 },
  "spawn_rate":{ "lv":0, "max":10, "cost0":30, "k":1.6, "delta":0.05 },
  "aura_regen":{ "lv":0, "max":5,  "cost0":35, "k":1.6, "delta":0.5 }
}
```
- 例コスト（四捨五入）: cost0=20 → **20/32/51/82**、cost0=30 → **30/48/77/123**。

### 23.2 訓練所（ちびの生成Lvの平均μだけ上げる）
```jsonc
{ "chibi_level_mean_bonus": { "steps": [0.5, 1.0, 2.0], "costs": [30, 60, 120] } }
```

### 23.3 作戦（ネームドの簡易ガンビット）
- **後衛固定／拠点守れ／一点集中** を**キャラごとに選択**（編集不可）。
```jsonc
{ "milly": "rear_safe", "mary": "rear_safe", "coco": "defend_base" }
```

### 23.4 ルーの店（恒久メタ）
```jsonc
{
  "shop": {
    "mana_gain_up_s":  { "lv":0, "max":10, "cost0":25, "k":1.7, "delta":0.05 },
    "mana_cap_up_s":   { "lv":0, "max":8,  "cost0":25, "k":1.7, "delta":50 },
    "good_title_rate": { "lv":0, "max":10, "cost0":20, "k":1.7, "delta":0.02 }
  },
  "consumables": {
    "mana_gain_token": { "cost":5, "gain_mul":1.10, "consume_on":"run_start" }
  }
}
```
- `mana_cap_up_s` は**ラン中に保持できる所持マナの上限**を恒久的に引き上げる（キャンプ口座とは別枠）。
- `mana_gain_token` は**ラン開始時に自動消費される一時バフ**（そのランの獲得マナ+10%）。**1個5マナ**で、購入しなければ一切バフが掛からないため縛りプレイも可能。

### 23.5 ショップ画面マウス対応（Camp限定）
- **目的**: **拠点⇄戦闘の橋渡しを完全マウス化**。`ショップ→購入→Deploy` がキーボード操作ゼロで完結。
- **対象範囲**: `CampScene` 全タブの**タブ切替・行選択・購入・Deploy**。他シーン（戦闘HUD等）は非対応のまま。

#### 23.5.1 操作要件
- **入力**: 左クリックのみで完結。ホイール/右クリック不要。
- **ホバー**: ポインタが行 or ボタン矩形に入ったら**ハイライト**（既存`selected`とは別に `hovered` 状態を持つ）。
- **クリック決定**:
  1. タブ矩形→ `setTab()` 呼び出し＋選択再Clamp。
  2. 行矩形→ `m_selection[idx]` を更新。**同じ行を押下→離す**と `handlePurchase()` 実行（disabled/affordableチェックは従来ロジック）。
  3. `Purchase` ボタン→現在タブに応じた `handlePurchase()`。
  4. `Deploy` ボタン→ `deploy()`（後述のUI配置を描画に追加）。
- **無効入力**: `disabled==true` 行 / 支払い不可行はクリックを受けても `pushFeedback("購入できません")` を出さず、ハイライトのみ抑制。

#### 23.5.2 レイアウト & ヒットボックス
- **タブ**: `renderTabs()` で生成する矩形を **`m_tabRects[4]`** に保存。描画後に `SDL_Rect{ x, y, width, height }` を保持しておき、イベント側で参照。
- **リスト行**: `renderRows()` で `RowHitbox` 配列を更新。
  ```cpp
  struct RowHitbox {
      SDL_Rect rect;
      int index;
      bool disabled;
  };
  std::vector<RowHitbox> m_rowHits;
  ```
  - 各行描画前に `rect` を計算→ `m_rowHits.push_back()`。
  - `hint` を描くため行の高さが伸びた場合は `rect.h` を加算しておく。
- **操作ボタン**: 右上テキストだった `"[Space] Deploy  [Enter] Purchase"` を**2つのボタン**に置換。
  - `Purchase`：一覧右上（Panel内上部）。`SDL_Rect` を `m_buttonPurchase` に保存。
  - `Deploy`：同エリア、`Purchase` の右隣。`SDL_Rect` を `m_buttonDeploy` に保存。
  - クリック処理は行と同様。`Deploy` は `m_battle` が存在し、`deploy()` が成功した場合のみ。

#### 23.5.3 イベントフロー
1. `SDL_MOUSEMOTION`
   - `m_pointer` に座標保持。
   - `hoveredTab`/`hoveredRow`/`hoveredButton` をヒットテストで更新し、描画時のカラー切替に使用。
2. `SDL_MOUSEBUTTONDOWN (Left)`
   - 押下時に `m_pressTarget = { type, index }` を記録（タブ/行/ボタン）。
3. `SDL_MOUSEBUTTONUP (Left)`
   - ボタンアップ位置が `m_pressTarget` と同じ矩形内なら確定処理を実行。
   - `m_pressTarget` は常にリセット。ドラッグ抜けた場合は処理しない（安全策）。

#### 23.5.4 視覚更新
- **ハイライト**:
  - タブ: 選択タブは従来配色、ホバー中タブは淡い紫（`rgba(90,70,130,200)`）。
  - 行: `hovered` で `selected` と別の縁取り。`selected && hovered` は明度を+10。
  - ボタン: 通常=暗紺、ホバー=明紺、押下中=さらに明るい。
- **カーソル**: `SDL_SetCursor` は不要。Windows/SteamDeck想定で標準矢印のまま。
- **フィードバックテキスト**: 場所・フォント変更無し。

#### 23.5.5 状態管理とセーブ
- 新規メンバ
  ```cpp
  std::array<SDL_Rect, 4> m_tabRects{};
  std::vector<RowHitbox> m_rowHits;
  SDL_Rect m_buttonPurchase{};
  SDL_Rect m_buttonDeploy{};
  struct PointerLatch { enum class Kind { None, Tab, Row, Purchase, Deploy } kind; int index; } m_pressTarget;
  int m_hoverTab = -1;
  int m_hoverRow = -1;
  PointerLatch::Kind m_hoverButton = PointerLatch::Kind::None;
  ```
- `update()`/`saveToDisk()` に影響無し。マウス状態はランタイムのみ保持し、セーブデータへは書き込まない。
- 既存ショートカット（Enter/Space/Zなど）は残し、マウス操作と併用可能。

#### 23.5.6 実装ステップ指針
1. `renderTabs/renderRows` の戻り値を `void` から `HitInfo` 返却に変更 OR 描画後にメンバ書き込みを加える。
2. `handleEvent` にマウス分岐を追加。SDLヘッダは既にインクルード済みのため追加不要。
3. `deployHint` テキストの代わりにボタン描画関数 `renderButtons()` を追加。
4. `RowDisplay`/`CampScene` への新規フィールドを初期化（コンストラクタ・`autoFocusAll` 実行時に `clearHits()`）。
5. 動作確認: タブ切替→購入→Deploy→Undo→再購入をマウスのみで通せること。

> **UX目標**: 店に入ってから「タブクリック → 目的の行クリック → Purchase → Deploy」を**4クリック以内・視線移動最小**で完了させる。

## 24) 経済チューニング（体験版・じわじわ強くなる）
- **1周マナ目安**: R1=~30 → R5=60–80 → R8=90–120（獲得+強化で**15–25%/run**伸び）。
- **クリア目安**: **6〜10周**（個人差あり）。
- **買い順ゴールデンパス**: `gain+5% (25)`→`base_hp (20)`→`μ+0.5 (30)`→`spawn+5% (30)`→`spawn+5% (48)`→`aura_regen (35)`→`cap+50 (25)`→`良称号+2% (20)`。
- **ROI指標**: 強化1つあたりの **+マナ/周 ≈ コストの10〜25%** を目安に。
- **消費型ブースト**: `mana_gain_token`（所持制限なし・ラン開始時に消費）で獲得マナ+10%。格安だが“縛り派”は買わずに挑める想定。

## 25) エリート雑魚 & ミニ実績（体験版）
### 25.1 エリート雑魚（色違い・稀）
```jsonc
{ "elite_rate": 0.05, "elite_bonus": { "hp_mul": 1.3, "atk_mul": 1.15, "mana_bonus": 3 } }
```
- 5%で出現。**HP×1.3／与ダメ×1.15／追加マナ+3**。見た目は**色Tint＋小冠**。

### 25.2 ミニ実績（クリア時清算）
```jsonc
[
  { "id":"low_panic",      "cond":"panic_ratio<0.30 && base_alive", "reward_mana":5 },
  { "id":"yuna_nohit_30s", "cond":"yuna_nohit>=30",                 "reward_mana":3 },
  { "id":"elite_kills_3",  "cond":"elite_kill>=3",                   "reward_mana":4 }
]
```
- 上限合計 **+12マナ**（毎周取り切れない想定で“再挑戦の動機”に）。


## 26) ループ / 再挑戦（体験版）
- **周回可**: ステージ1は**クリア後も何度でも挑戦可能**（敗北後も同様）。
- **リザルト→選択肢**:
  - **[R] もういっかい挑戦**（キャンプを挟まず即再戦。*獲得マナは自動で口座に加算*）
  - **[Enter] キャンプへ戻る**（既定フォーカス）
  - **[Esc] タイトルに戻る**
- **持ち越し（恒久）**: 拠点強化／ルーの店（メタ）／訓練所μ／ネームドLv／作戦プリセット選択／マナ残高。
- **リセット（周回ごと）**: マップ状態（破壊物・効果床・封鎖フラグ）／スポーンタイマー／ユウナ/ネームドHP・CD／ラン統計（Panic比率・実績カウンタ）。
- **ミニ実績**: 各周回で**毎回判定・毎回付与**（クリア時清算）。
- **所持マナ**: ラン中は `mana_cap` まで保持可能。`mana_cap_up_s` でこの上限を恒久的に拡張し、リザルト画面到達時に残マナはすべて自動で口座へ振替される。
- **消費型ショップ品**: `mana_gain_token` は購入分をストックし、**次のラン開始時に1個自動消費**してそのランの獲得マナ+10%。使わずに縛りプレイをすることも可能。
- **セーブ**: リザルト画面到達時に**自動セーブ**（マナ・購入履歴・作戦）。

**フロー定義（叩き台）**
```jsonc
// run_flow.json
{
  "on_victory": [ "result_screen", "autosave", "await_choice" ],
  "on_defeat":  [ "result_screen", "autosave", "await_choice" ],
  "choices": {
    "retry":  { "hotkey":"R",     "ops":["bank_mana","reset_run","load_stage1"] },
    "camp":   { "hotkey":"Enter", "ops":["bank_mana","go_camp"] },
    "title":  { "hotkey":"Esc",   "ops":["bank_mana","go_title"] }
  },
  "persistence": ["camp_upgrades","shop_meta","training_mu","named_levels","ai_presets","wallet_mana"],
  "reset": ["map_state","spawn_state","hp_and_cds","run_stats"]
}
```


## 21-A) 補足: ユウナ喪失時の全体ペナルティ（ちびわふ）
- **条件**: `on_commander_down` かつ **味方拠点が1つ以上**残っている。
- **効果**: **ちびわふ全員が「強制Panic」**に入る（**ユウナ復活まで**）。
  - 強制Panic中は**攻撃禁止**、**「とっこう」分岐は抑制**（=発動しない）。
  - 「すがる（ゆうなにすがる）」は**ユウナ不在のため自動で「かいてん（拠点退避）」へフォールバック**。
- **解除**: `on_commander_revive`（ユウナ復活）で**全ちびの強制Panicフラグを解除**し、通常の性格ロジックへ復帰。

**イベント差分（叩き台）**
```jsonc
{
  "events": {
    "on_commander_down": [
      { "if": "ally_base.count==0", "do": "defeat()" },
      { "if": "ally_base.count>0",  "do": "apply_to('chibi','set_flag(force_panic,true)')" },
      { "if": "ally_base.count>0",  "do": "set_state(defend_base_fallback)" }  // ネームドは拠点退避
    ],
    "on_commander_revive": [
      { "do": "apply_to('chibi','set_flag(force_panic,false)')" }
    ]
  }
}
```

## 27) ちびわふ性格 → Panic分岐（4系統）
**二軸**: **ゆうかん**（-2..+2）: びびり/ひかえめ/ふつう/ゆうかん/ちょとつもうしん、**ちせい**（-2..+2）: おばか/ぽやん/ふつう/なやみがち/かんがえるこ。

**正規化**: `b=(yuukan+2)/4`、`s=(chisei+2)/4`。

### 27.1 閾値と係数（式）
- **泣き逃げHP**: `flee_hp = lerp(0.60,0.15,b)`
- **最低Panic時間**: `flee_min_s = lerp(2.0,0.8,b)`
- **再ターゲット遅延**: `retarget_lock_s = lerp(0.8,1.8,s)`
- **AoE回避倍率**: `aoe_avoid_mul = lerp(0.85,1.30,s)`
- **追従半径**: `escort_r_px = round(84 - 20*s)`
- **群れ係数**: `cohesion_k = lerp(0.28,0.12,s)`

### 27.2 Panic発火条件
- `hp_ratio ≤ flee_hp` でPanicに遷移。Panic中の**基本**は攻撃禁止。

### 27.3 分岐と優先順位
1) **とっこう**（ゆうかん≥+1 かつ ちせい≤0）→ **攻撃許可** / **2.5s** / 与ダメ+15% / 被ダメ+30% / 移動+10% / 攻撃間隔-10%。
2) **かいてん**（ちせい≥+1）→ **拠点退避**。拠点オーラ半径128px内で **hp≥min(0.85, flee_hp+0.25)** または **4.0s**で解除→ **2.0sだけきょてんまもる**にバイアス。
3) **すがる**（ゆうかん≤-1 かつ ちせい≥0）→ **ユウナ背面150°/半径36–56px**に貼り付き。**攻撃禁止**。`hp≥flee_hp+0.20` または **オーラ内2.0s**で解除。
4) **にげまどう**（ちせい≤-1 ほか）→ **0.8s逆走 + 0.2sジグザグ**のループ。`hp≥flee_hp+0.20` または **オーラ内2.0s**で解除。

**強制Panic中の特例**
- **とっこうは無効化**（発動しない）。
- **すがる**はユウナ不在のため**自動でかいてんへ**。

### 27.4 JSON（叩き台）
```jsonc
// personality_panic.json
{
  "branch": {
    "tokkou_if": "yuukan>=1 && chisei<=0 && !self.flag.force_panic",
    "base_if":   "chisei>=1",
    "cling_if":  "yuukan<=-1 && chisei>=0",
    "run_if":    "chisei<=-1"
  },
  "retreat_base": {
    "aura_radius_px": 128,
    "hp_ok": "min(0.85, flee_hp + 0.25)",
    "min_stay_s": 4.0
  },
  "run_around": { "dash_s": 0.8, "jitter_s": 0.2, "repath_s": 0.5 },
  "cling": {
    "rear_deg": 150,
    "radius_min_px": 36,
    "radius_max_px": 56,
    "prefer_opposite_of_nearest_enemy": true,
    "aoe_avoid_bonus_mul": 1.10,
    "no_attack": true,
    "exit_hp_add": 0.20,
    "exit_in_base_aura_s": 2.0
  },
  "tokkou": {
    "dur_s": 2.5,
    "prio_target": ["enemy_base_nearby", "nearest_enemy"],
    "buffs": { "atk_mul": 1.15, "speed_mul": 1.10, "cd_mul": 0.90 },
    "risk":  { "taken_mul": 1.30 },
    "max_simul": 3
  }
}
```

---
## 28) ユウナ魔法リネーム & 数値調整（体験版）
- **リネーム**: 旧「こかげのひ」→ 新「**ファイアボール**」
- **入力更新**: **U = ファイアボール**（内容を下記に置換）
- **方針**: **ミリーのファイアボールより弱い**（威力/回転ともに下位）。

### 数値（ユウナのファイアボール）
- **タイプ**: 単体・非貫通・弾速**160px/s**
- **Dmg**: **4**　**CD**: **2.4s**　**射程**: **120px**　**MP**: **10**
- 備考: **ミリー**は `Dmg6 / CD0.9s / 射程110 / 弾速180 / MP消費なし`（＝明確に上位）

### JSON差分（実装向け）
```diff
// yuna.json（skillsセクション）
-    "ember":       { "type":"proj",  "damage":6, "cd_s":2.0, "mp":10, "range_px":120, "speed_px_s":180, "pierce":0 },
+    "fire_ball":   { "type":"proj",  "damage":4, "cd_s":2.4, "mp":10, "range_px":120, "speed_px_s":160, "pierce":0 },
```

> 併せてテキストUI（入力説明）も **U=ファイアボール** に表記更新すること。


## 28-A) ガード仕様 更新（体験版）
- **入力**: **K長押し＝ガード**（離すと解除）。
- **防御内容**: **前方160°**の近接/飛び道具/ドラゴン扇形を**ダメージ0**・**ノックバック無効**。**床DoT（毒/炎床）**は軽減なし。
- **移動**: **移動不可**（その場で向きだけ変えられる）。**ダッシュ/スキル/号令**はロック。
- **MP消費（秒間なし／被弾依存）**: 旧仕様の**6MP/秒**は**廃止**。
  - **ヒット毎**に **`1 MP`** 消費（固定）**＋** **「防いだダメージ」×`0.20` MP** を加算。端数は**切り上げ**。
  - 例: ゴブリンDmg12 → **1 + ceil(12×0.20)=1+3=4MP**／ゴーレムDmg35 → **1+7=8MP**。
  - **連続/持続ヒット（扇形炎など）**は**`0.2s`ウィンドウ**で合算して1回のヒットとして計算。
- **パーフェクトガード（PG）**: ガード成立から**0.18s以内**に被弾なら、
  - **固定1MPを免除**（=ベース0）し、**総コスト×0.5**に低減。
  - 雑魚のみ**0.2sひるみ**（ボスは無効）。
- **ガードブレイク**: MP0で被弾した瞬間、**0.6sスタン**＋**2.0s再ガード不可**。その後は通常へ。
- **MP再生**: ガード中も**通常のMP再生**（1.5/s、拠点内+0.5/s）は**有効**。
- **備考**: 旧仕様の**移動速度0.5倍**・**秒間消費**は撤廃。

### JSON差分（`yuna.json`）
```diff
-    ,"guard": {
-      "type": "guard_hold",
-      "front_arc_deg": 160,
-      "startup_s": 0.10,
-      "endlag_s": 0.10,
-      "move_mul": 0.5,
-      "negate": ["melee","projectile","cone"],
-      "block_env_dot": false,
-      "mp_drain_s": 6.0,
-      "perfect_window_s": 0.18,
-      "perfect_mp_refund": 6,
-      "minor_stagger_on_trash_s": 0.2,
-      "break_on_mp0": { "stun_s": 0.6, "relock_s": 2.0 },
-      "lock": { "dash": true, "skills": true, "orders": true }
-    }
+    ,"guard": {
+      "type": "guard_hold",
+      "front_arc_deg": 160,
+      "startup_s": 0.10,
+      "endlag_s": 0.10,
+      "move_mul": 0.0,
+      "negate": ["melee","projectile","cone"],
+      "block_env_dot": false,
+      "mp_cost": { "base_per_hit": 1, "per_damage": 0.20, "cone_window_s": 0.2, "round": "ceil" },
+      "perfect_window_s": 0.18,
+      "perfect_rules": { "waive_base": true, "cost_mul": 0.5, "minor_stagger_on_trash_s": 0.2 },
+      "break_on_mp0": { "stun_s": 0.6, "relock_s": 2.0 },
+      "lock": { "dash": true, "skills": true, "orders": true, "move": true }
+    }
```

---
（ここでおわり）
